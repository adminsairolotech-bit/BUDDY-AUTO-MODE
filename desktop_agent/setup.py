from setuptools import find_packages, setup


setup(
    name="openclaw-desktop-agent",
    version="1.0.0",
    description="Desktop automation agent for OpenClaw clone",
    packages=find_packages(),
)
